[build]
builder        = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"

[deploy]
startCommand            = "node_modules/.bin/prisma migrate deploy && echo '=== STARTING NODE ===' && node dist/main.js 2>&1"
restartPolicyType       = "ON_FAILURE"
restartPolicyMaxRetries = 3
