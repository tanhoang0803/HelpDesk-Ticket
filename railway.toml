[build]
builder        = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"

[deploy]
startCommand      = "npx prisma migrate deploy && npx prisma db seed && node dist/main"
restartPolicyType       = "ON_FAILURE"
restartPolicyMaxRetries = 3
