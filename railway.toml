[build]
builder        = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"

[deploy]
startCommand            = "node_modules/.bin/prisma migrate deploy && node dist/main"
restartPolicyType       = "ON_FAILURE"
restartPolicyMaxRetries = 3
